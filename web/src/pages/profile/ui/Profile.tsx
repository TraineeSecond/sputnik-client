import { UserDetails } from 'entities';
import { MainLayout } from 'widgets';

const Profile = () => {
  return (
    <MainLayout>
      <UserDetails />
    </MainLayout>
  );
};

export default Profile;
