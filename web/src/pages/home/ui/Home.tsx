import { MainLayout } from 'widgets';
import HomeView from 'widgets/HomeView/ui/HomeView';

const Home = () => {
  return (
    <MainLayout>
      <HomeView />
    </MainLayout>
  );
};

export default Home;
