import { RegisterForm } from 'shared';
import { AuthLayout } from 'widgets';

const Register = () => {
  return (
    <AuthLayout>
      <RegisterForm />
    </AuthLayout>
  );
};

export default Register;
